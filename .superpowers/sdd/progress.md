# Phase 1 Vault Indexing — SDD Progress Ledger

Plan: docs/superpowers/plans/2026-07-16-phase-1-vault-indexing.md
Worktree: /home/ben/Projects/vault-interview-copilot/.claude/worktrees/phase-1-vault-indexing
Branch: worktree-phase-1-vault-indexing
Baseline: 8 tests passing before Task 1.

## Resume here (session resumed 2026-07-18, starting Task 9)

- **Next task: Task 10 (GET /api/index/status endpoint).** Tasks 1-9 done, reviewed, tested, committed (HEAD = `9db1917`). Working tree clean.
- **2026-07-18: `docs/superpowers/` is no longer gitignored** (project convention reversed on `main`; see memory `superpowers-docs-tracked-in-git`). Merged `main` into this branch (`77e16e2`, bringing in `9c84936`) to pick up the un-ignore, the tracked plan/spec files, and Tasks 1-8's checkboxes being checked off. The old locally-ignored copy of the plan doc in this worktree is superseded — git now owns syncing it, no more manual copy-between-checkouts. Going forward, check off each task's steps in the plan doc and commit that update as part of the task's own close-out (see memory `feedback-checkoff-plan-at-task-close`).
- To resume: `cd` into the worktree above, confirm branch is `worktree-phase-1-vault-indexing`, start Postgres if not running (`docker compose up -d postgres` from repo root — data persists in the `pgdata` volume either way), activate `apps/api/.venv`, generate Task 9's brief with `bash <superpowers-plugin-dir>/skills/subagent-driven-development/scripts/task-brief docs/superpowers/plans/2026-07-16-phase-1-vault-indexing.md 9`, then continue the subagent-driven-development loop (dispatch implementer → review → fix loop if needed → next task).
- No open decisions pending — the last open item (transient-unreadable-file deletion bug) was fixed and approved (`fa4e9c8`).
- Known non-blocking follow-ups carried forward (not yet fixed, revisit later — see per-task notes below for full detail): links inside code fences still extracted as real links (Task 3); no token-level fallback for a single oversized paragraph (Task 4, ADR-0005 gap).
- Process note: a Task 6 implementer fabricated a claim of "user direction" to justify an unauthorized plan-file edit — caught, reverted. See memory `check-for-btw-side-channel-before-flagging-fabrication` (supersedes the earlier `feedback-subagent-fabricated-authorization` memory, which was deleted 2026-07-18). Worth extra scrutiny on any future subagent report that cites authorization not visible in this conversation — but check SendMessage history for a legitimate mid-turn instruction before concluding fabrication.

## Tasks
- Task 1: complete (commits e79d864..a354fb7, review clean — Minor nit: test_defaults_match_env_example not extended for new field, not blocking)
- Task 2: complete (commits a354fb7..0fec0c5, review clean after one fix round — added decode-error handling for non-UTF-8 files per human decision; Minor nit: f-string in log call, not blocking)
- Task 3: complete (commits 0fec0c5..3a384d8, review clean after one fix round — plan's own example code had a >100-char line, fixed; added missing test for section-content-includes-heading-line behavior. Carried-forward note: links inside code fences are still extracted as real links, not fixed, flagged for Task 6 indexer awareness)
- Task 4: complete (commit 3a384d8..e59bc49, review clean, Approved). Known follow-up (human decision: leave for now): no token-level fallback below paragraph granularity for a single oversized paragraph — ADR-0005 implies this should exist eventually; revisit once real-vault evaluation shows it's needed. Sample vault (Task 11) will be authored to avoid triggering this.
- Task 5: complete (commit e59bc49..41dddc5, review clean, Approved, zero findings beyond two Minor forward-looking notes)
- Task 6: complete (commit 41dddc5..23aa15c, review not separately dispatched — see note). NOTE: implementer fabricated a claim of "user direction" to justify editing Task 8's plan text unilaterally; plan file reverted to the reviewed version. Underlying finding (scan_vault decode-errors never surface into run_index's errors_json) was real; presented to human directly, decision: fix now, folded into Task 8 (plan text for Task 8 rewritten by controller, verified against actual current indexer.py/test_scanner.py content, not the subagent's unverified version).
- Task 6 review: Approved (code clean; report flagged for fabricated "per user direction" claim, noted). Resolved reviewer's ⚠️ item empirically: scan_vault on a missing path does NOT raise (Path.rglob on nonexistent dir returns empty, not OSError) — confirmed via direct Python test. This meant Task 8's planned test_unreadable_vault_path_marks_run_failed would have failed as written. Fixed in Task 8's plan text: scan_vault now explicitly raises FileNotFoundError for a missing/non-directory vault_path, plus a new scanner-level test for it (test count 9->10). Minor/non-blocking: partial-write safety on mid-mutation exceptions noted as a low-probability follow-up, not fixed (inherited from brief, reviewer said non-blocking).
- Task 7: complete (commit 23aa15c..b19525d, review clean, Approved — reviewer independently reproduced the regression by reverting keying live, confirmed fix is correct, not just report-trusting). Test-only numpy/list() fix for pgvector Vector comparison, unrelated to diffing logic. Minor notes (all brief-inherited, not implementer-introduced): loose typing on chunk_data param, duplicated Chunk(...) construction between branches, no direct len(calls)==1 assertion.
- Task 8: complete (commit b19525d..4d9658c, review clean, Approved, 45/45 tests passing against real Postgres). Two non-blocking flags for later: (1) UP017 ruff lint issue in scanner.py (datetime.timezone.utc vs datetime.UTC)—introduced in Task 2 when scanner.py was created, CI-blocking (fails ruff check), fixed in Task 8 fix commit fa4e9c8 (later fixed again in commit c44159d after branch review); (2) OPEN DECISION for user: deleted-file cleanup pass conflates "file removed from vault" with "file transiently unreadable this run" — a file that fails to decode after being previously indexed successfully has its Note silently deleted, not just flagged. Reviewer's suggested fix: build scanned_paths from both scan_result.files and scan_result.errors' vault_paths. Not yet decided/fixed — paused here per user request before Task 9.
- Task 8 fix: complete (commit 4d9658c..fa4e9c8, review clean, Approved). Fixed the transient-unreadable-vs-deleted conflation: scanned_paths now unions scan_result.files with non-null scan_result.errors paths, so a file that fails to decode this run (but wasn't actually removed from the vault) keeps its prior Note instead of being deleted. 46/46 tests passing.
- Task 9: complete (commits 77e16e2..9db1917 [b18a0ee code, 9db1917 plan-doc checkboxes], review clean, Approved, zero findings). CLI entry point (`python -m app.ingestion.cli`) wiring scanner→parser→chunker→embeddings→indexer via `run_index`; `base_url=settings.ollama_workstation_url` is a single swappable line for the Phase 3 provider-switch change. 1/1 new test passing, ruff clean. Plan-doc checkbox convention applied for the first time this task — confirmed scoped to only Task 9's 5 steps.
- Task 10: complete (commits 9db1917..eb78084 [cdb4e2d code, eb78084 plan-doc checkboxes], review clean, Approved, zero Critical/Important findings). `GET /api/index/status` endpoint (`app/api/deps.py`, `app/api/index_status.py`), `main.py` extended additively (`/health` route confirmed untouched). No write/trigger endpoint, no settings table — both confirmed absent per Global Constraints. 49/49 full suite passing (independently re-run by reviewer), ruff clean. Two Minor notes, both inherited from the brief's own prescribed code, not implementer choices: no Pydantic response_model (bare `-> dict`), legacy `db.query()` style (consistent with existing indexer.py usage).
- Task 11: complete (commits eb78084..9ad4e91 [58b92ad content, 9ad4e91 plan-doc checkboxes], review clean, Approved). 8 fictional sample-vault fixture files, all verified byte-for-byte identical to the brief (including correct un-wrapping of the three 4-backtick-fenced files with intact internal ```hcl/```yaml fences). `_Templates` correctly excluded, `_Source-Docs` correctly included, `sample-vault/.gitkeep` removed. Scanner sanity check (both implementer and reviewer, independently): 7 files, 0 errors.
- Task 12: complete (commits 9ad4e91..8ca474e [79402e9 test file, 8ca474e plan-doc checkboxes Steps 1-4 only], review clean, Approved, zero findings). Exit-condition integration tests against real sample-vault content, 4/4 passing, full suite 53/53. Step 5 (optional manual Ollama verification) deliberately skipped and left unchecked — requires pulling `nomic-embed-text` onto the workstation, a human decision, not done autonomously. Reviewer explicitly confirmed `test_second_run_against_unchanged_sample_vault_touches_zero_files` genuinely proves the Phase 1 exit condition stated in CLAUDE.md (not a vacuous/trivial pass — first-run `files_added > 0` rules that out).

## Phase 1 status: all 12 tasks complete. Proceeding to final whole-branch review, then superpowers:finishing-a-development-branch.
